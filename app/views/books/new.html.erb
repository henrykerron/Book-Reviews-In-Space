<h1> Add a New Book</h1>

<%= form_for @book do |f| %>
	<%= f.label :title %> 
	<%= f.text_field :title %> <br/>

	<%= f.label :description %> 
	<%= f.text_field :description %> <br/>

	<%= f.label :url, "URL" %> 
	<%= f.text_field :url %> <br/>

	<%= f.submit "Add Book" %> <br/>
<%end%>